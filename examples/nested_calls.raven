// Test nested function calls and call stack depth

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn multiply(a: i32, b: i32) -> i32 {
    return a * b;
}

fn compute(x: i32, y: i32, z: i32) -> i32 {
    // Test nested function calls: multiply(add(x, y), z)
    return multiply(add(x, y), z);
}

fn main() -> i32 {
    let result = compute(5, 3, 2);  // (5 + 3) * 2 = 16
    return result;
}
